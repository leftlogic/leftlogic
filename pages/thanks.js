import Layout from '../components/Layout';

export default () => {
  return (
    <Layout title="Contact Left Logic" sidebars={['Newsletter', 'Terminal']}>
      <h1>Thank you</h1>
      <p>Again, all enquires are read and remain private.</p>
      <p>We will be in touch within the next few days.</p>
    </Layout>
  );
};
